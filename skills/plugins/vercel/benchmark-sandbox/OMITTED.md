This Vercel plugin eval harness (`run-eval.ts`, ~78KB) was omitted from the public snapshot.

It is not a SKILL.md. The upload was blocked because the file references `process.env.ANTHROPIC_API_KEY` and `VERCEL_TOKEN` (environment variable names, not secret values). The full file still lives in the local plugin cache.
